[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
evm_version = "paris" # Compatible with standard EVM / Monad

# Monad Testnet RPC Configuration
[rpc_endpoints]
monad_testnet = "https://testnet-rpc.monad.xyz"
